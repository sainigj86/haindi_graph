import 'package:flutter/material.dart';

class SuppliderDetailsScreen extends StatelessWidget {
  const SuppliderDetailsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text('data')
      ],
    );
  }
}